extends ../../_layouts/base-eng

block config
  //- Correct base url pathing for this page
  - var baseUrl = '../../'

block content
    main.areas
        section.areas__nav
            h1.section-title Practice Areas
            ul.areas__nav__links
                each item, index in site.data.areasEng
                    li(data-area-target="page-#{index}")= item.title
        section.areas__pages
            each page, index in site.data.areasEng
                .areas__page__wrapper(data-area-page="page-#{index}")
                    .areas__page
                        each block in page.pages[0]
                            p!=block
                        if page.pages[1].par
                            each p in page.pages[1].par
                                p= p
                        p Some of the main services provided by this area are:
                        ul.areas__page__servicios
                            each item in page.pages[1].servicios
                                li=item
                        p
                            strong Team:
                        dl.areas__page__asociados
                            each item in page.pages[1].asociados
                                dt= item.nombre
                                dd
                                    a(href="/docs/cvs/#{item.link}.pdf")   
                                        | CV [
                                        i.material-icons arrow_downward
                                        | ]
                        ._spacer
            
            
